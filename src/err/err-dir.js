//(function(window, angular, app, undefined) {
    'use strict';

    var deparam = require('jquery-deparam');

    angular.module('err.err-dir',[
    ])
    // .directive('errDir', function() {
    // })
  ;
//})(window, window.angular, window.app);
